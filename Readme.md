Create PDF slideshows based on Inkscape SVG layers
==================================================

## Ubuntu installation instructions

- Make sure Python is installed
- Install the `python-setuptools` package
- Install by typing `sudo python setup.py install`